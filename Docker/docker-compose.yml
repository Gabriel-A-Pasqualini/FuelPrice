version: "3.9"

services:
  flutter:
    build: .
    container_name: flutter_dev
    working_dir: /app
    volumes:
      - .:/app
      - ~/.android:/root/.android
    network_mode: host
    stdin_open: true
    tty: true
    